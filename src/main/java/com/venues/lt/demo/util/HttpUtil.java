package com.venues.lt.demo.util;

public class HttpUtil {
    // JSON.parseArray(JSON.parseObject(json).getString("studentList"), Student.class)
    // JSON.parseObject(user,UserDto.class);
    //JSONObject = JSON.parseObject(string);

//    public static void main(String[] args) {
//        float fa = 345076.80f;
//
////        DecimalFormat decimalFormat = new DecimalFormat("0.00");// 构造方法的字符格式这里如果小数不足2位,会以0补足.
////        String amount = decimalFormat.format(fa);// format 返回的是字符串
////        System.out.println(amount); //这里输出345076.81
////
////        BigDecimal bigDecimal2 = new BigDecimal(Float.toString(fa));
////        String amountBd = decimalFormat.format(bigDecimal2);// format 返回的是字符串
////        System.out.println(amountBd);//这里输出345076.80
//        //在java里要使用BigDecimal 类型来处理，同时实例化BigDecimal 时要使用String来中转一下
//    }
}
